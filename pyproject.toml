[project]
name = "easy-a2t"
version = "1.0.0"
description = "Convert audio file to text with Wisper."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "faster-whisper>=1.2.1",
    "psutil>=7.2.2",
    "python-multipart>=0.0.26",
    "qrcode[pil]>=8.2",
]
